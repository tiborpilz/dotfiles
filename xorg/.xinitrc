#!/bin/bash


## Colorscheme
#wal_status=$(wal -R -q 2>&1)

##Create display manager daemon
#compton_status=$(compton --dbus -b 2>&1)
#echo "(XPROFILE) $(date): $compton_status" >>/home/tibor/startup.log
