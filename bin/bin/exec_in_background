#!/bin/bash
SESSION=background
WINDOW=${SESSION}:1
PANE=${WINDOW}.0

CMD=$(echo "" | rofi -dmenu -p " > "); $HOME/bin/exec_in_background "${cmd}"

tmux has-session -t $SESSION || tmux new-session -d -s $SESSION

tmux send-keys -t $PANE "$CMD"
tmux send-keys -t $PANE "Enter"
